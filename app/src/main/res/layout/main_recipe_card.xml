<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/recipe_item_image"
        android:layout_width="match_parent"
        android:src="@drawable/ic_broken_image_black_24dp"
        android:layout_height="@dimen/card_height"
        android:layout_marginEnd="@dimen/card_margin_horizontal"
        android:layout_marginLeft="@dimen/card_margin_horizontal"
        android:layout_marginRight="@dimen/card_margin_horizontal"
        android:layout_marginStart="@dimen/card_margin_horizontal" />

    <TextView
        android:id="@+id/recipe_item_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/card_height"
        android:layout_marginEnd="@dimen/card_margin_horizontal"
        android:layout_marginLeft="@dimen/card_margin_horizontal"
        android:layout_marginRight="@dimen/card_margin_horizontal"
        android:layout_marginStart="@dimen/card_margin_horizontal"
        android:background="@color/main_recipebackground"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:textAlignment="center"
        android:textColor="@color/main_recipetext"
        android:textSize="@dimen/card_text_size"
        android:shadowColor="@color/main_recipetext_shadow"
        android:shadowDx="-2"
        android:shadowDy="-2"
        android:shadowRadius="5"
        tools:text="Chocolate Brownies" />


</RelativeLayout>